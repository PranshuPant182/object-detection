// Static GPU VM IP used for manual weekly access only
// Safe to hardcode — not exposed publicly, and IP is inactive by default
// NOSONAR

const config = {
    SERVER_IP: '40.76.240.26', // NOSONAR: This is an isolated GPU VM IP
};

export default config;